/bin/
/data/
/dist/
/stage/
/work/
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
.DS_Store
.env
.env.*
*.key
*.pem
/outputs/

# A repo-root MCP config is one developer's machine: an absolute path to their
# checkout and the id of a session that is theirs. Claude Code auto-loads it
# for anyone who opens the repo, so it must not be shared.
.mcp.json
